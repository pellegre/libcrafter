version: 1
kind: feature
name: wifi_vht_ldpc
summary: VHT20 SU LDPC rate-matching geometry with explicit signaling consistency.
layers: [dot11]
directions: [backend_to_libcrafter]
strict_bytes: true
malformed: true
behaviors:
  - name: vht20-su-ldpc-iq
    source: IEEE 802.11-2020 21.3.10.5.4, 21.3.10.9.2 and 21.3.20
    checks: [tone_permutation, service_crc, exact_psdu, aggregate_fcs, both_guard_intervals, extra_symbol_group, bounded_streaming, independent_iq]
  - name: vht20-su-ldpc-geometry
    source: IEEE 802.11-2020 21.3.10.5.4 Equations 21-61 through 21-63 and 19.3.11.7.5
    checks: [encoded_phy_padding, extra_symbol_flag, symbol_grouping, bounded_dimensions, shortening, puncturing, repetition, independent_geometry, exact_recovery]
coverage_cases: [wifi-vht20-ldpc-rate]
supported_cases:
  - {name: wifi-vht20-ldpc-rate, directions: [backend_to_libcrafter], byte_policy: strict_bytes, contract_only: true}
backend_support:
  libcrafter:
    status: partial
    notes:
      - Independent forward geometry covers 20412 cases and Gaussian parity encoding supplies 54 exact recovery bitstreams.
      - Complete SU SISO IQ reception covers 73 independent waveforms; VHT STBC/MU and hardware qualification remain outside this increment.
      - SERVICE and per-MPDU FCS remain mandatory even when partial LDPC estimates recover an unaffected aggregate member.
